import { StyleSheet } from "react-native";

import {
	BACKGROUND_COLOR,
} from "./../../config/styles";

export default StyleSheet.create({
	screen: {
		flex           : 1,
		backgroundColor: BACKGROUND_COLOR,
	},
});
